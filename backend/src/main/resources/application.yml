server:
  port: 8080

spring:
  # ===== 定时任务线程池 =====
  task:
    scheduling:
      pool:
        size: 4
      thread-name-prefix: lottery-scheduled-

  # ===== 默认 SQLite =====
  # 切换 MySQL 只需改下面 datasource 配置即可
  datasource:
    url: jdbc:sqlite:${LOTTERY_DB_PATH:./data/lottery.db}
    driver-class-name: org.sqlite.JDBC
    hikari:
      maximum-pool-size: 10

  # ===== MySQL 配置（取消注释即可切换）=====
  # datasource:
  #   url: jdbc:mysql://${MYSQL_HOST:127.0.0.1}:${MYSQL_PORT:3306}/${MYSQL_DB:lottery}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
  #   username: ${MYSQL_USER:root}
  #   password: ${MYSQL_PASSWORD:}
  #   driver-class-name: com.mysql.cj.jdbc.Driver

  sql:
    init:
      # SchemaMigrationRunner 已处理建表，不需要 Spring 自动执行 schema.sql
      mode: never
      schema-locations: classpath:schema.sql

mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    com.lottery: DEBUG
    root: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%level] %msg%n"

lottery:
  fetch:
    cron: "0 30 22 * * ?"
  cors:
    allowed-origins: "*"
  audit:
    created-by: "system"
    updated-by: "system"
  # ===== 鉴权配置 =====
  # 设置 token 后，写操作（POST /fetch）和推荐接口需携带 Authorization: Bearer {token}
  # 读取接口（GET /status, /analyze, /results 等）无需鉴权
  # 留空或不配置则不启用鉴权
  auth:
    token: ${LOTTERY_AUTH_TOKEN:}
